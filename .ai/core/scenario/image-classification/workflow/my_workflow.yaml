name: my-scenario
description: Example scenario for testing
inputs:
  prompt:
    type: string
    default: "Hello from SAP AI Core!"
  temperature:
    type: number
    default: 0.7
    optional: true
  max_tokens:
    type: integer
    default: 100
    optional: true
workflow:
  type: ai-core-workflow-template
  id: foundation-models
